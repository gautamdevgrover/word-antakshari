# Copy to .env. Use a unique URL-safe password before deploying.
HTTP_PORT=8080
POSTGRES_DB=word_antakshari
POSTGRES_USER=word_antakshari
POSTGRES_PASSWORD=change_me_local_only
# Must match the browser-facing URL, including port (no trailing slash).
APP_ORIGIN=http://localhost:8080
